class Programa079 {
  constructor() {
    const r = 5.2;
    const area = Math.PI * r * r;
    const per = 2 * Math.PI * r;
    console.log(`Radio: ${r}`);
    console.log(`Área: ${area}, Perímetro: ${per}`);
  }
}

const programa = new Programa079();